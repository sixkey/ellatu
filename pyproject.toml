[tool.pyright]
include = ["scr"]
executionEnvironments = [
  { root = "src" }
]

[build-system]
requires = [
    "setuptools>=42",
    "discord", 
    "pillow", 
    "tatsu",
    "numpy", 
    "wheel", 
    "pymongo", 
    "pygraphviz", 
]
build-backend = "setuptools.build_meta"
